import { TotalUnreadPipe } from './total-unread.pipe';

describe('TotalUnreadPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalUnreadPipe();
    expect(pipe).toBeTruthy();
  });
});
