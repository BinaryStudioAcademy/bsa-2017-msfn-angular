export class ISport {
    constructor(
        public name: string,
        public description: string,
        public icon: string,
        public code?: number,
        public id?: string
    ) { }
}
